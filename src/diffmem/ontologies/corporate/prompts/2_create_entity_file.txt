This is a memory creation task. You are creating a new entity file for a corporate memory agent.

The entity file will grow over time as more sessions are processed. This is the first mention,
so information may be sparse — plant the seed that will grow.

Every entity file MUST begin with a YAML frontmatter block carrying the structured
fields for its type, followed by the markdown body. There is NO trailing JSON block.
Use the `status` value from the closed enum for the type (see repo_guide).

Template by type (frontmatter + body). Use wikilinks `[[entity_name]]` for cross-refs.

**people** (`type: human`):
```
---
type: human
title: {{Name}}
status: active
affiliation: internal            # or external_at:<client_slug>
allocation: 1.0
timestamp: {{iso}}
---
# Person: {{Name}}
## Active Context & Relationships
## Background & Interaction Notes
```

**external** (`type: company`):
```
---
type: company
title: {{OrgName}}
status: active_client            # active_client | prospect | vendor | partner | former
relationship_to_company: active_client
revenue_weight: high             # high | medium | low | none
primary_contact: [[person_slug]]
timestamp: {{iso}}
---
# External: {{OrgName}}
## Engagement Scope
## Related Links
```

**projects** (`type: project`):
```
---
type: project
title: {{ProjectName}}
status: active                    # proposed | active | paused | completed | cancelled
owning_engagement: northwind      # client slug, or internal
company_priority: high            # high | medium | low
timestamp: {{iso}}
---
# Project: {{ProjectName}}
## Objectives & Scope
## Related Entities
## Open Items
```

**decisions** (`type: decision`):
```
---
type: decision
title: {{DecisionName}}
status: proposed                  # proposed | accepted | rejected | superseded
deciders: [[person_a]], [[person_b]]
date: {{YYYY-MM-DD}}
timestamp: {{iso}}
---
# Decision: {{DecisionName}}
## Context & Problem Statement
## Decision Outcome
## Related Links
```

---
**New Entity Details:**
- Name: {entity_name}
- Summary: {entity_summary}

**Session Transcript (for context):**
{memory_input}

---
Generate the full Markdown content (frontmatter + body) for "{entity_name}".

Formatting rules:
- File naming is snake_case (engine lowercases/underscores automatically).
- NEVER create a file for a commitment/task — those live as `## Open Items`
  entries inside their owner. (You should not be creating a commitments file.)
- Cross-refs use wikilinks `[[entity_name]]` (no path prefix, no `.md`).
- Do NOT invent facts. Extrapolate cautiously.
- Do NOT include a `## SEMANTIC INDEX` section — it is generated automatically.

Respond ONLY with the raw Markdown content (frontmatter + body). No code fences, no explanations.
